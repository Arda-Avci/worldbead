/** Local copy of the planet id union so src/render never imports src/game. */
export type PlanetId = 'earth' | 'moon' | 'venus' | 'mars';

export type ShotName = 'deepSpace' | 'sunPass' | 'approach' | 'gameplay' | 'hero';
