node_modules/
dist/
*.log
.DS_Store
.env
.env.*
# Native build outputs
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
ios/App/build/
ios/App/Pods/
ios/App/App/public/
ios/DerivedData/
*.keystore
*.jks
# Fixed debug keystore, committed on purpose so every CI build is signed
# with the same key and installs over the previous build.
!android/app/debug.keystore
